# 复制为 backend/.env 或通过环境变量注入。
# JWT_SECRET_KEY 必须为强随机值（至少 32 字符），禁止使用任何占位符/公开值，
# 否则后端拒绝启动。生成方式：
#   python -c "import secrets; print(secrets.token_urlsafe(48))"
JWT_SECRET_KEY=REPLACE_WITH_A_RANDOM_SECRET_AT_LEAST_32_CHARS_LONG
JWT_EXPIRE_HOURS=24

# 其余配置项与根目录 .env.example 一致。